coverage:
  precision: 1

  status:
    project:
      default:
        target: auto
    patch: off
    changes: off

comment:
  layout: "header, diff, grid"
  behavior: default
  require_changes: false

ignore:
  - "R/db_connections.R"
